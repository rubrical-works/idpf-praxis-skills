{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "heuristic-checklist-schema.json",
  "title": "Heuristic checklist (for --audit-checklist)",
  "description": "Validator for bundled and user-supplied heuristic checklists. The audit pass walks the checklist and produces a finding per heuristic against the recommended artefact.",
  "type": "object",
  "required": ["checklistId", "name", "heuristics"],
  "properties": {
    "checklistId": { "type": "string", "minLength": 1 },
    "name": { "type": "string", "minLength": 1 },
    "source": { "type": "string", "description": "Citation for the checklist (book, standard, URL)" },
    "applicabilityNote": { "type": "string", "description": "When this checklist is the right fit for an artefact" },
    "heuristics": {
      "type": "array",
      "minItems": 5,
      "items": {
        "type": "object",
        "required": ["id", "name", "description"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
          "name": { "type": "string", "minLength": 1 },
          "description": { "type": "string", "minLength": 1 }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
